@model SecondChance.Models.Comment

@{
    ViewData["Title"] = "Excluir Comentário";
}

<h1>Excluir Comentário</h1>

<div class="alert alert-warning">
    <h3>Tem certeza que deseja excluir este comentário?</h3>
</div>

<div class="card mb-4">
    <div class="card-body">
        <h5 class="card-title">Comentário de @Model.Author.FullName</h5>
        <h6 class="card-subtitle mb-2 text-muted">Em @Model.CreatedAt.ToString("dd/MM/yyyy HH:mm")</h6>
        <p class="card-text">@Model.Content</p>
    </div>
</div>

<form asp-action="Delete">
    <input type="hidden" asp-for="Id" />
    <input type="submit" value="Excluir" class="btn btn-danger" />
    <a asp-action="ProfileComments" asp-route-profileId="@Model.ProfileId" class="btn btn-secondary">Cancelar</a>
</form> 